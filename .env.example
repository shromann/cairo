# Copy to .env for local development (core/config.py loads it automatically).
# Local Postgres, bypasses the Cloud SQL connector:
DATABASE_URL=postgresql+pg8000://cairo_app:dev@localhost:5432/cairo
APP_ENV=development
# Cloud SQL instead: leave DATABASE_URL unset and `source env.sh` (SQL_CONNECTION, DB_USER, DB_PASSWORD, DB_NAME).
